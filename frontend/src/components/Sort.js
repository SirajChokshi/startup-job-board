import React, { Component } from 'react';

export default class Sort extends Component {
  state = {
  }

  render () {
      return (
        <div>
           <h1> Sort </h1>
        </div>
      )
   }
}
